name: Publish Package

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+-*'

jobs:
  publish:
    name: Publish to npm Registry
    runs-on: ubuntu-latest
    permissions:
      contents: write       # Required for creating releases
      id-token: write        # Required for OIDC
      attestations: write    # Required for artifact attestations
    
    defaults:
      run:
        working-directory: cli

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - run: npm ci
      - run: npm run build --if-present
      - name: Do copy
        run: |
          cp -r ../skills ./skills
          cp -r ../agents ./agents
      - run: npm test
      
      # - name: Update version in package.json
      #   run: |
      #     VERSION="${{ github.ref_name }}"
      #     VERSION="${VERSION#v}"
      #     npm version "$VERSION" --no-git-tag-version

      # - run: npm publish --provenance --access public --otp=${{ secrets.NPM_OTP }}
      #   env:
      #     NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      # - name: Download and attest published tarball
      #   run: |
      #     VERSION="${{ github.ref_name }}"
      #     VERSION="${VERSION#v}"
      #     curl -sL "https://registry.npmjs.org/@mammals-at-work/yacs/-/yacs-${VERSION}.tgz" -o "yacs-${VERSION}.tgz"

      # - name: Generate artifact attestation
      #   uses: actions/attest-build-provenance@v2
      #   with:
      #     subject-path: cli/yacs-*.tgz

      # - name: Create Release
      #   uses: softprops/action-gh-release@v1
      #   with:
      #     files: cli/package.json
      #     body: |
      #       # Release ${{ github.ref_name }}

      #       Published to npm as `@mammals-at-work/yacs`.

      #       ## Usage
      #       ```bash
      #       npx @mammals-at-work/yacs
      #       ```

      #       Or clone the repository and run:
      #       ```bash
      #       cd cli
      #       npm install
      #       npm start
      #       ```